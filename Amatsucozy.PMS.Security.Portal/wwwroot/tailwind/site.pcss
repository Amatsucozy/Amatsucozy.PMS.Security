html, body {
    @apply h-full;
}

body {
    @apply m-0;
}

* {
    @apply box-border transition-all duration-300 ease-in-out !important;
}

*:not(span[class="material-icons-round"]) {
    @apply font-sans !important;
}

h1 {
    @apply text-2xl;
}

a {
    @apply text-inherit no-underline;
}
